# Declaring variables

- Always use (const) 
- If that's not possible think again!
- and again...
- ...ok use (let)

```jsx
const username = 'jamalbelilet'
const authenticated = false
const count = 33
count = count + 4 // error!
// ----------
let count = 0
count = 88 // no errors!
```
